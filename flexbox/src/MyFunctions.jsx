import React from "react";
// what it does is to identify it as a React environment.

function Sum(a,b){
   // perform your logic here. 
   //import react should be the first thing in a react environment. NOTE: If it is just JS there won't be need for that you can just maintain your - function parenthesis (){}.
   //This is then followed by - export default parenthesis
   return <h1>Welcome</h1>;
   // with return, you can write into DOM
}
 
export default Sum

